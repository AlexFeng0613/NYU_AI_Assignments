This program only contains 1 .java file with the name main.

To run this program the only thing need to do is type in command: javac main.java, then type: java main. The program will starts in this way.

This program will instructs what to input: Ncard, next Ltarget then Utarget. The two matrix will display the content of play and the probablity. play matrix will tell whether Draw or Pass has a higher winning rate.

That's the whole introduction for this program. Thanks for compiling and grading!